name: exercise_engine
description: Deterministic, UI-independent exercise analysis for FitVision AI.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
