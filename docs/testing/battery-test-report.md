# Battery test report

No controlled battery run was performed. Device model, Android version, initial/final battery, duration, screen state, GPS/camera mode, network/map state, foreground-service state and thermal condition must be recorded for every future observation. One short run cannot support an hourly-consumption or optimization claim.
