# Insight rules

Stable rules cover same-exercise valid-form improvement/stability, full-range share, unique active days, running distance and weighted pace. Priorities are form, pace, full range, consistency, distance, then stable context. Maximum output is four, codes are deduplicated, and UI text comes from fixed code templates. Missing samples produce a neutral “more sessions needed” state, never a negative claim.
