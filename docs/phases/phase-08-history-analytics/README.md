# Phase 8 — History and deterministic analytics

Phase 8 projects completed workout and running rows into one local-first history, calculates calendar-period analytics from Drift, and produces explainable rule-coded insights. No LLM, prompt, embedding, random selection or generated text is used.
