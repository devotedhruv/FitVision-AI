# Privacy

Routes are sensitive. Collection is limited to an explicitly started run and stops at finish. Only calculated run records and accepted route points are retained; no location is sent to analytics or production logs. Records are user-scoped, access tokens never enter SQLite payloads, and API ownership derives from the authenticated token. Public sharing is not included.
