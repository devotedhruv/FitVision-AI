# Metrics

Only completed sessions contribute. Workout aggregates are authoritative because Phase 6 transactionally updates them with rep insertion; details use rep rows and aggregates are never added to rep totals again. Failed/active/abandoned/deleted sessions are excluded. Form score remains null because Phase 5 currently does not claim a reliable score; valid-form ratio is used instead.
