# Unified history

History supports all-time/today/7-day/30-day/custom periods, exercise/running category, exercise type and sync state. Filters combine in SQL. Pages contain 25 rows ordered by `started_at DESC, local_id DESC`; full rep and route relationships are excluded from cards. Pull-to-refresh reloads local truth, including unsynced rows.
