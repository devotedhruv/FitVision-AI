# Threshold changelog

## Rules 1.0.0 — 2026-07-26

Phase 5 defaults were captured in `ai/reference_rules/*.json`; the runtime Dart configuration remains the authoritative executable source and every snapshot records that path. No threshold changed in Phase 9 because no consented calibration plus locked-holdout evidence was available. Future entries must list old/new values, failing anonymous IDs, expected effect, regression result and unchanged holdout result.
