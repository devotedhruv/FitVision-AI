# exercise_engine

Pure Dart, deterministic pose-landmark analysis for FitVision AI. The package
accepts normalized landmarks and has no Flutter, camera, MediaPipe, network, or
storage dependency. Threshold defaults are calibration starting points, not
medical or biomechanical conclusions.

Run with `dart test packages/exercise_engine` from the repository root.
