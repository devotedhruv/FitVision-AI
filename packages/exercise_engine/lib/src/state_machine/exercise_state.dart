enum ExerciseType { squat, bicepsCurl, pushup }

enum BodySide { left, right }

enum ExerciseState {
  ready,
  standing,
  descending,
  bottom,
  ascending,
  extended,
  flexing,
  contracted,
  extending,
  top,
  paused,
  unknown,
}
