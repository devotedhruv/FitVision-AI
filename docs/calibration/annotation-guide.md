# Annotation guide

Annotations follow `ai/evaluation/schemas/annotation.schema.json`: anonymous sample ID, exercise, split, expected complete/incomplete counts, timed rep ranges, form labels, visibility-loss intervals, camera/distance/lighting/speed/device categories and optional non-identifying notes. A second reviewer should resolve ambiguous start/end ranges before locking holdout labels.
